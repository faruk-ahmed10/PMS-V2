import React from 'react';
import AlertDialog from "../../Layouts/Components/Global/AlertDialog/AlertDialog";

const GlobalComponents = (): Required<React.ReactNode> => {
    return (
        <React.Fragment>
            <AlertDialog />
        </React.Fragment>
    );
};

export default GlobalComponents;
